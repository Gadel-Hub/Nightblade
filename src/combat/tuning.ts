// Seconds and internal pixels. Attack range is measured from the body edge.
export const COMBAT = {
  startupDuration: 0.08,
  activeDuration: 0.10,
  recoveryDuration: 0.18,
  attackRange: 24,
  attackHitboxWidth: 24,
  attackHitboxHeight: 18,
  attackDamage: 1,
  targetHealth: 3,
  durableTargetHealth: 10,
} as const;
